# LAST MILE v0.2 · 当前实装与验收

日期：2026-09-16。当前代码位于项目根 `client/`、`server/`、`scripts/`、`tests/`；游戏版本由根 `package.json` 定义。

## 当前结论

已完成一名玩家操作的三幕护送游戏、中英双语、英文默认、3D 沙盘与 2D 场景、调查／溯源／上传／AI 顾问、路线与结果分支、独立行为复盘及导出。两套隐藏案例与信息隔离规则已实现。

当前仍是本机可玩版本。OpenAI / Anthropic 适配器已实现；OpenAI `gpt-5.6-luna` 已完成真实中英文合成测试及有限的英文领域运行链路测试。最新全仓回归为 13 个文件、192 项通过，生产构建通过；本机服务已于 2026-09-16 重启为 OpenAI 模式。详见[真实模型接入验收](真实模型接入验收.md)。这不等于真实模型完整三关浏览器验收、稳定性保证或云端验收；Anthropic 仍只有模拟协议测试。

## v0.2 初次发布验收（历史里程碑）

| 检查 | 结果 |
| --- | --- |
| TypeScript 与生产构建 | 通过 |
| Vitest | 10 个文件、158 项通过 |
| Playwright | 4 条流程通过，54.2 秒 |
| 界面截图 | 21 张，中英桌面、390px 窄屏、情报、来源图、复盘 |
| 生产页面抽查 | 默认英文和语言切换通过，无未处理页面异常 |
| 真实付费模型调用 | 此阶段为 0；后续结果见上述真实接入验收 |

原始机器记录见 [verification.json](../releases/playable_v0.2/verification.json)，详细范围见 [双语版本说明](双语版本说明.md)。中文真实时间流程实际等待 30 秒入场；两条完整三幕流程在测试进程注入时钟，浏览器/API 操作保持真实。没有给玩家开放调速或跳关接口。

目录整理后的构建、测试与 Git 排除检查保存在本机 `docs/repository-audit/2026-09-16-verification.json`，该目录不上传 GitHub；旧验收不冒充本轮重跑。

## 仍需完成

1. 在已验证接口的基础上，完成真实模型参与的完整三关浏览器试玩，继续评估双语质量、证据引用、实际等待与费用；有限合成测试不能替代这些检查。
2. 云端访问配置、邀请门禁、独立访客身份与游戏归属校验、全站模型配额、持久磁盘及备份。
3. 根据团队实际试玩校准时间和调查额度；当前规则仍为 `design_preview`。
4. 当前故事在抵达接收点时结束，没有假造完整人员交接完成记录；无配音，尚无跨启动历史列表。
5. 行为画像用于本局学习复盘，不代表已验证的心理测量工具。

当前技术栈与部署方向见 [部署方案与技术栈](部署方案与技术栈.md)。v0.1 的 145 项测试、仅中文等历史状态保存在本机 `archive/releases/playable_v0.1/验收结果.md`，不上传 GitHub，也不再作为首页状态。
